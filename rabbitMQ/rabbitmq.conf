## MQTT listeners
mqtt.listeners.tcp.1 = 0.0.0.0:1883
mqtt.listeners.ssl.1 = 0.0.0.0:8883
mqtt.allow_anonymous = true
mqtt.vhost            = /
mqtt.exchange         = amq.topic
mqtt.default_user     = iot_pipeline
mqtt.default_pass     = changeme

## Internal service account (Bento MQTT + IoT Agent AMQP)
## TODO K8s: inject via Secrets
default_user = iot_pipeline
default_pass = changeme

## TLS (for device-facing port 8883)
ssl_options.cacertfile = /etc/rabbitmq/certs/ca.crt
ssl_options.certfile   = /etc/rabbitmq/certs/server.crt
ssl_options.keyfile    = /etc/rabbitmq/certs/server.key
ssl_options.verify     = verify_peer
ssl_options.fail_if_no_peer_cert = true

log.file.level = debug