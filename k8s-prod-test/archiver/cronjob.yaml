apiVersion: batch/v1
kind: CronJob
metadata:
  name: cratedb-archiver
spec:
  schedule: "0 0,12 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          nodeSelector:
            kubernetes.io/arch: arm64
          containers:
            - name: archiver
              image: droidsagencyairegistry.azurecr.io/gps-archiver:latest
              imagePullPolicy: Always
              env:
                - name: CRATE_HOST
                  value: cratedb
                - name: CRATE_PORT
                  value: "4200"
                - name: MINIO_ENDPOINT
                  value: http://minio:9000
                - name: MINIO_ROOT_USER
                  valueFrom:
                    secretKeyRef:
                      name: gps-connector-secrets
                      key: MINIO_ROOT_USER
                - name: MINIO_ROOT_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: gps-connector-secrets
                      key: MINIO_ROOT_PASSWORD
                - name: ARCHIVE_BUCKET
                  value: gps-archive
                - name: ARCHIVE_AFTER_MINUTES
                  value: "10080"
                - name: DELETE_AFTER_MINUTES
                  value: "20160"
          restartPolicy: Never
